{
  "name": "babel-node-boilerplate",
  "version": "1.0.0",
  "description": "Minimal setup to start writing modern NodeJS apps right now.",
  "main": "src/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "start": "nodemon src/index.js --exec babel-node",
    "serve": "node dist/index.js -r dotenv/config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henrique Silverio",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:HenriqueSilverio/babel-node-boilerplate.git"
  },
  "dependencies": {
    "dotenv": "^6.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/node": "^7.0.0",
    "@babel/preset-env": "^7.1.0",
    "nodemon": "^1.18.4"
  }
}
